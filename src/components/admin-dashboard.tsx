/* eslint-disable react-hooks/set-state-in-effect */
"use client";
import Link from "next/link";
import { useEffect,useState } from "react";
import { CheckCircle2,FileSpreadsheet,MessageSquareMore,ShieldAlert,XCircle } from "lucide-react";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";
type RequestRow={id:string;status:string;submitted_at:string|null;requirements:Record<string,boolean>;completed_requirements:number;staff_notes:string|null;reward_accounts:{display_name:string}|null};
export default function AdminDashboard(){const [access,setAccess]=useState<"checking"|"allowed"|"denied">("checking");const [rows,setRows]=useState<RequestRow[]>([]);async function load(){const s=getSupabaseBrowserClient();if(!s)return;const {data}=await s.from("verification_requests").select("id,status,submitted_at,requirements,completed_requirements,staff_notes,reward_accounts(display_name)").order("submitted_at",{ascending:false});setRows((data??[]) as unknown as RequestRow[])}useEffect(()=>{const s=getSupabaseBrowserClient();if(!s){setAccess("denied");return}s.auth.getUser().then(async({data})=>{if(!data.user){setAccess("denied");return}const {data:p}=await s.from("profiles").select("role").eq("id",data.user.id).single();if(p?.role==="staff"||p?.role==="admin"){setAccess("allowed");await load()}else setAccess("denied")})},[]);async function review(id:string,status:"approved"|"rejected"|"needs_information"){const notes=window.prompt("Staff notes (optional)")||null;const {error}=await getSupabaseBrowserClient()!.rpc("review_reseller_application",{p_request_id:id,p_status:status,p_staff_notes:notes});if(error)window.alert(error.message);else await load()}if(access==="checking")return <main className="import-page">Checking staff access…</main>;if(access==="denied")return <main className="import-page"><section className="import-panel"><ShieldAlert/><h1>Staff access required</h1><Link href="/">Return to dashboard</Link></section></main>;return <main className="import-page"><section className="import-shell"><span className="auth-eyebrow">Onward staff</span><h1>Administration</h1><p className="import-intro">Staff tools are separate from the customer dashboard.</p><Link className="primary-button" href="/admin/import"><FileSpreadsheet size={17}/>Open CSV Import Center</Link><h2 className="admin-section-title">Reseller applications</h2><div className="admin-request-list">{rows.length===0?<p>No applications submitted.</p>:rows.map(r=><article className="admin-request" key={r.id}><div><strong>{r.reward_accounts?.display_name??"Rewards account"}</strong><span>{r.status} · {r.completed_requirements}/3 requirements · {r.submitted_at?new Date(r.submitted_at).toLocaleDateString():"Not submitted"}</span><small>License: {r.requirements.license?"Yes":"No"} · Decorator: {r.requirements.decorator?"Yes":"No"} · Spend: {r.requirements.spend?"Yes":"No"}</small></div><div className="admin-request-actions"><button onClick={()=>void review(r.id,"approved")}><CheckCircle2 size={15}/>Approve</button><button onClick={()=>void review(r.id,"needs_information")}><MessageSquareMore size={15}/>Request info</button><button onClick={()=>void review(r.id,"rejected")}><XCircle size={15}/>Reject</button></div></article>)}</div></section></main>}
